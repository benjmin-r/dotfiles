abbr -a vim nvim
abbr -a vi  nvim

abbr -a l exa -la
abbr -a lt exa -la --git --group-directories-first --sort modified

abbr -a tx tmuxifier
abbr -a ghi 'TERM=xterm-256color ghi'
abbr -a sync-contacts "PYTHONPATH='' vdirsyncer sync and mates index"
